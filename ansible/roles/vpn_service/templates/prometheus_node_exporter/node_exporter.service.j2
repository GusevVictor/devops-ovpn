[Unit]
Description=Node Exporter
After=network.target

[Service]
User={{ prometheus_user }}
Group={{ prometheus_user }}
Type=simple
ExecStart=/usr/local/bin/node_exporter --web.config.file="/etc/prometheus/web.yml" --collector.systemd

[Install]
WantedBy=multi-user.target

